import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View,SafeAreaView } from 'react-native';

export default function App() {
  return (
    //SafeAreaView = a tela comeca depois do relogio, wifi,baterria
    <SafeAreaView style={styles.container}> 
      <Text>Open up App.js to start working on your app!</Text>
      {/* <StatusBar style="auto" /> */} 
      {/* aqui ele pega 100% da pagina se digitar algo fica embaixo do relogio*/}
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center', //deixa a linha centralizada na linha
    justifyContent: 'center', //deixa no meio da tela do celular
  },
});
